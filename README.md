# cs2-generate-position-binds
Python scripts for generating CS2 config files that cycle through a list of positions with the mouse buttons.

# Configs
`configs/` contains pre-generated spawn cycles for both sides of the active duty maps plus cache: `ancient`, `anubis`, `cache`, `dust2`, `inferno`, `mirage`, `nuke`, `overpass`, `vertigo`.
